/**
 * Exercício 1: Análise de Vendas por Categoria
 *
 * Suponha que você tenha uma lista de vendas de uma loja online com informações sobre categorias de produtos.
 * Cada venda é representada por um objeto com as seguintes propriedades:
 * { categoria: string, quantidade: number, preçoUnitario: number }
 *
 * Realize as seguintes operações:
 *
 * 1. Filtrar as vendas de uma categoria específica.
 * 2. Mapear cada venda para o total obtido por aquela venda (quantidade * preçoUnitario).
 * 3. Usar reduce() para calcular o total de vendas dessa categoria.
 */

/**
 * Exercício 2: Análise de Despesas Mensais
 *
 * Suponha que você tenha uma lista de despesas mensais de uma empresa.
 * Cada despesa é representada por um objeto com as seguintes propriedades:
 * { mês: string, valor: number }
 *
 * Realize as seguintes operações:
 *
 * 1. Filtrar as despesas de um mês específico.
 * 2. Mapear cada despesa para seu valor.
 * 3. Usar reduce() para calcular o total das despesas desse mês.
 */

/**
 * Exercício 3: Análise de Alunos Aprovados
 *
 * Suponha que você tenha uma lista de notas de alunos.
 * Cada aluno é representado por um objeto com as seguintes propriedades:
 * { nome: string, nota: number }
 *
 * Realize as seguintes operações:
 *
 * 1. Filtrar os alunos aprovados (nota maior ou igual a 7).
 * 2. Mapear os alunos aprovados para seus nomes.
 * 3. Usar reduce() para contar o número total de alunos aprovados.
 */

/**
 * Exercício 4: Análise de Horas Trabalhadas
 *
 * Suponha que você tenha uma lista de registros de horas trabalhadas por funcionários.
 * Cada registro é representado por um objeto com as seguintes propriedades:
 * { funcionario: string, horas: number }
 *
 * Realize as seguintes operações:
 *
 * 1. Filtrar os registros de um funcionário específico.
 * 2. Mapear os registros para o número de horas trabalhadas.
 * 3. Usar reduce() para calcular o total de horas trabalhadas por esse funcionário.
 */

/**
 * Exercício 5: Análise de Vendas por Região
 *
 * Suponha que você tenha uma lista de vendas de uma empresa com informações sobre regiões.
 * Cada venda é representada por um objeto com as seguintes propriedades:
 * { regiao: string, quantidade: number, preçoUnitario: number }
 *
 * Realize as seguintes operações:
 *
 * 1. Filtrar as vendas de uma região específica.
 * 2. Mapear cada venda para o total obtido por aquela venda (quantidade * preçoUnitario).
 * 3. Usar reduce() para calcular o total de vendas dessa região.
 */
